include ../components/data.pug
include ../components/mixin.pug


doctype html
html(lang='ru')
    head
        meta(charset='UTF-8')
        meta(name='viewport' content='width=device-width, initial-scale=1.0')

        block vars

        block pageTitle
            title= siteTitle

    body
        +header(navLinks, currentPage)
        block content
        +footer()